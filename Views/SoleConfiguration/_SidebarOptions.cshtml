@model WebApplication1.Models.SoleConfiguration

<h5>Gravure</h5>
<p>@Model.EngravingText</p>

<h5>Finition</h5>
<ul class="list-unstyled">
    <li>Envoyer au client: @(Model.SendLinkToClient ? "Oui" : "Non")</li>
    <li>Prototype autorisé: @(Model.AllowPrototypeRequest ? "Oui" : "Non")</li>
</ul>

<button class="btn btn-primary">Enregistrer</button>
<button class="btn btn-success">Envoyer la demande</button>